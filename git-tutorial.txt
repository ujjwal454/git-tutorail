git init ->  initializes new git reposetry
 
 command -> git init



we have untracked unmodified  modified  staged files 


git add -> it adds the file from untracked to the staging file


command for add -> git add "file name"  (quotes are not required)
git add all -> it add all files from untracked to the staging area
command to add all -> git add --a

git commit -> it takes the snapshot of the file and after that it makes our file as unmodified
than if we do something in our file it will be move to modified file 

command to commit -> git 


untracked ->  we are not tracking the file with the help of git 

unmodified -> if we are tracking a file and we have not changed anything to our code is falls into unmodified file

modified -> if we are tracking a file and we have updated that file than this comes under modified case


how to create a new file in the git hub repo using git 

using touch command
touch "file name" (quotes are not required)




how to back to the previous version of the file 

git checkout "file name" (quotes are not required)

how get all the previous version file 

git checkout -f 



how to check who have commited the file 
 by using git log 
 command -> git log



if we want to chcek last one 

by using git log -p-1 
instead of 1 we can write any number according to iur need 



diff command

diff command gives all the changes we have made after the last commit


command to commit the file without using the commit command
git commit -a -m




how to delete a file from a working repo

we can do this using rm command

git rm "file name"


command to remove the file from the git repo but we want to keep  on the hard disk 


get rm "file name" --cached


